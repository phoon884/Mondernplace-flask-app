setup:
  build:
    docker:
      web: nginx/dockerfile
      worker: dockerfile
  config: 
    APP_ENV: "prod"
    APP_NAME: "webserver"
    APP_DEBUG: "true"
    SERVICE_NAME: "webserver"
  run:
    worker: gunicorn --bind 0.0.0.0:5000 app:app
